export enum AnimationState {
  Initial = 'initial',
  WebDevTools = 'webdev',
  Frameworks = 'framework',
  Backend = 'backend',
  Tooling = 'tooling',
}

export enum KeyRotation {
  Clockwise = "rotate(90deg)",
  Counterclockwise = "rotate(-90deg)",
  None = "rotate(0)"
}